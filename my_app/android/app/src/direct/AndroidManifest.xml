<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- DIRECT (website) build only. The Play build never includes this, so
         Google Play review is not affected by the install permission. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application>
        <!-- Serves the downloaded update APK to the system installer via a
             content:// URI (required on Android 7+). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>
